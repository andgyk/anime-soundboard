{
  "name": "anime-soundboard",
  "homepage": ".",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "deploy": "yarn export && touch out/.nojekyll && git add out/ -f && git commit -m \"Deploy gh-pages\" && git subtree push --prefix out origin gh-pages",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "12.1.6",
    "next-optimized-images": "^2.6.2",
    "react": "18.1.0",
    "react-dom": "18.1.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "eslint": "8.16.0",
    "eslint-config-next": "12.1.6",
    "postcss": "^8.4.14",
    "touch-cli": "^0.0.1"
  }
}
